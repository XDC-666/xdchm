# 本地开发用环境变量（把本文件复制为 .env 后填写；.env 已被 .gitignore 忽略，不会上传）

# ⚠️ JWT 签名密钥：本地与生产都必须配置，缺失时服务会拒绝启动（安全加固）。
# 用一段足够随机的长字符串，例如：  openssl rand -hex 32
JWT_SECRET=请改成一段随机长字符串例如xq_3f9a2b7c1d8e4f6a9b0c

# 服务端口（本地默认 3000）
PORT=3000

# 允许跨域调用后端的前端来源（逗号分隔）。
# 默认已包含 localhost 与 GitHub Pages；若用 Cloudflare Tunnel 等临时地址，请在此追加，例如：
# CLIENT_ORIGIN=https://creativity-mathematics-collar-crops.trycloudflare.com
CLIENT_ORIGIN=
